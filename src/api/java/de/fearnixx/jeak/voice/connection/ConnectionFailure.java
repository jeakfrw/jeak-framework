package de.fearnixx.jeak.voice.connection;

/**
 * Enum that contains the reasons why a {@link IVoiceConnection} could not connect to the server
 */
public enum ConnectionFailure {
    TIMEOUT,
    UNSPECIFIED
}
